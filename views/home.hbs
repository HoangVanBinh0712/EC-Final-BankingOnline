<div class="mt-4">
    <div class="row">
        <div>{{cour}} </div>
        {{#each cour}}
            <div>{{this}} </div>
            <div>{{this.amount}} </div>

        {{/each}}
    </div>
</div>